@AGENTS.md

Claude Code should use `AGENTS.md` as the shared engineering baseline and then read the repository's existing project-specific rules, docs, ADRs, code, tests, configuration, CI/release conventions, deployment procedures, and Git state for the task at hand.

Do not duplicate project policy here. If Claude-specific runtime behavior needs a durable instruction later, keep it small and tool-specific.
